// Step 1: Declare an array called toDoList with three initial tasks
let toDoList = ["Wake up", "Brush teeth", "Have breakfast"];

// Step 2: Add a new task, "Go for a walk", to the end of the array
toDoList.push("Go for a walk");

// Step 3: Remove the last task from the array
toDoList.pop();

// Step 4: Print the updated array
console.log(toDoList);
